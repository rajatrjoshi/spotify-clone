export  const initialState = {
    user: null,
    playlists: [],
    playing: false,
    itme: null
}

const reducer = (state, action) => {
console.log(action);

    switch(action.type) {
        
    }
}

export default reducer;